# 📝 Fullstack Todo App

A modern **Fullstack Todo Application** built with **React (Vite + TypeScript)** on the frontend and **Node.js + Express + MongoDB (Mongoose)** on the backend.  
This project demonstrates CRUD operations, validation with **Zod**, and a clean folder structure suitable for professional projects.

---

## 🚀 Features

- ✅ Create, Read, Update, and Delete Todos
- 🧩 TypeScript on both frontend and backend
- 🧠 Form validation using **Zod** + **React Hook Form**
- 🎨 UI built with **Shadcn/UI** + **Tailwind CSS**
- 🌍 Global API handler for fetch requests
- ⚙️ Environment-based configuration (via `.env`)
- 🧾 MongoDB with Mongoose models and timestamps

---

## 📁 Folder Structure

## 🧑‍💻 Getting Started

### 1. Clone the repository

```bash
git clone https://github.com/janaka99/hiring-fullstack-todo.git


```

### 2. Start the server

```bash

cd hiring-fullstack-todo/server

```

### 3. Add needed environment variables to `.env` file (Details on `server/.env.example`)

### 4. Start the server

```bash

npm run build

npm run start

```

### 5. Starting the Client

#### Add new terminal

```bash

cd hiring-fullstack-todo/client

npm install

```

### 6. Run the application

```bash

npm run dev

```
